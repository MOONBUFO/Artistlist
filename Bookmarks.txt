javascript:function extractIdFromPageSource() {    const htmlSource = document.documentElement.innerHTML;    const regex = /\/artists\/(\d+)/;    const match = htmlSource.match(regex);    return match ? match[1] : %27%27;}function loadCSVAndFindNextLine(id) {    const cachedData = localStorage.getItem(%27myCSVData%27);    if (cachedData) {            findNextLine(cachedData, id);    } else {            const url = %27https://raw.githubusercontent.com/MOONBUFO/Artistlist/main/Artistlist.js%27;        fetch(url)            .then(response => {                if (!response.ok) {                    throw new Error(%27Network response was not ok%27);                }                return response.text();            })            .then(csvText => {                            localStorage.setItem(%27myCSVData%27, csvText);                            findNextLine(csvText, id);            })            .catch(error => {                console.error(%27There has been a problem with your fetch operation:%27, error);            });    }}function findNextLine(csvText, id) {    const rows = csvText.split(%27\n%27);        let targetRow = null;    for (let i = 0; i < rows.length; i++) {        if (rows[i].includes(id)) {            targetRow = i;            break;        }    }        if (targetRow !== null && targetRow + 1 < rows.length) {        const nextLine = rows[targetRow + 1].trim();                     const index = nextLine.indexOf(%27,%27);        const tags = nextLine.substring(index + 1).trim();                    const redirectUrl = `https://danbooru.donmai.us/posts?tags=${encodeURIComponent(tags)}`;                    window.location.href = redirectUrl;                console.log(`Redirecting to: ${redirectUrl}`);        return tags;    } else {        console.warn(`No valid line found after ID ${id} in CSV.`);        return %27%27;    }}const id = extractIdFromPageSource();loadCSVAndFindNextLine(id);